---
"@neovici/cosmoz-omnitable": minor
---

Add a `primary` column option

`<cosmoz-omnitable-column primary>` marks the column that identifies the row
(for example the supplier or customer). Its cells render in `text-primary`
and medium weight, as the row title, while the other columns stay
`text-secondary`.
